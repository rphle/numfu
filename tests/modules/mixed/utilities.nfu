const VERSION = "1.0.0"
const factorial = {n -> if n <= 1 then 1 else n * factorial(n - 1)}
const computedConstant = add(5, 10) * 2

export add = {a, b -> a + b}
export VERSION, factorial, computedConstant
